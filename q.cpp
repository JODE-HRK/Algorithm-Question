#include <bits/stdc++.h>
#define ll long long
using namespace std;
const int maxn = 3010;
int n, k;
int a[maxn];
int main()
{
    scanf("%d %d", &n, &k);
    for (int i = 1; i <= n; i++)
    {
        scanf("%d", &a[i]);
    }
    sort(a + 1, a + 1 + n);
    
}